// Function related to a rectangle, they don't touch the dom, they are pure functions that work with the parameters. I'll probably also need to test them
export const rectangleArea = (width, height) => {
    return width * height;
};

export const rectPerim = () => {};
